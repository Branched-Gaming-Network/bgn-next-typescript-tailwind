/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "",
    output: "export",
    distDir: "dist",
    images: {
        unoptimized: true,
    }
};

export default nextConfig;
